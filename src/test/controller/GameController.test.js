import { jest, test, expect } from "@jest/globals";
import GameController from "../../controller/GameController";
import Ship from "../../entities/Ship";
import Coordinate from "../../entities/Coordinate";


test('add valid ships', () => {
    // const mockGameController = new GameController();

    // const ship1 = new Ship(new Coordinate(1, 1), true, 5);
    // const ship2 = new Ship(new Coordinate(5, 1), true, 5);
    // const ship3 = new Ship(new Coordinate(6, 1), true, 5);
    // const ship4 = new Ship(new Coordinate(1, 2), false, 5);
    // const ship5 = new Ship(new Coordinate(4, 3), true, 5);

    // expect(mockGameController.placeShip(ship1, true, 5)).toBeTruthy();

    // expect(mockGameController.placeShip(ship1, true, 5)).toBeFalsy();
    // expect(mockGameController.placeShip(ship2, true, 5)).toBeFalsy();
    // expect(mockGameController.placeShip(ship3, true, 5)).toBeFalsy();

    // expect(mockGameController.placeShip(ship4, false, 5)).toBeTruthy();
    // expect(mockGameController.placeShip(ship5, true, 5)).toBeTruthy();
});